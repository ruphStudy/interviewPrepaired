/** Template lifecycle status — own domain enum, mirrors the other institute sub-resource statuses' shape only by coincidence. */
export enum InstituteInterviewTemplateStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
