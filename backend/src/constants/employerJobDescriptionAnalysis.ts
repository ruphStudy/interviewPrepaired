/** Lifecycle of one AI-parsed structured analysis of a single JD source version (17B). */
export enum EmployerJobDescriptionAnalysisStatus {
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
