/** Trainer profile lifecycle status — distinct from the underlying OrganizationMember's own status (identity/access), this only reflects the profile metadata's own state. */
export enum InstituteTrainerProfileStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
