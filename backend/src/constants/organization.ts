/** Organization lifecycle status — generic, not tied to any specific organization type (institute/company come in Prompt 7B). */
export enum OrganizationStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  ARCHIVED = 'archived',
}
